/* Auto-generated from legacy/index.original.html */
export const indexScripts: string[] = [
  "(function(){if(sessionStorage.getItem('home_auth_v1')!=='yes')document.documentElement.classList.add('gate-locked');})();",
  "if(sessionStorage.getItem('home_auth_v1')==='yes')document.getElementById('gate').classList.add('hide');",
  "// ══ HOME PASSWORD GATE ══\n  localStorage.removeItem('home_auth_v1');\n  const HOME_AUTH_KEY = 'home_auth_v1';\n  /** Must match for-alaa.html — rotate both together. Not secret from anyone who views source. */\n  const HOME_PASS = 'alaa-core';\n  const homeGate = document.getElementById('gate');\n  let gateLang = 'en';\n\n  function toggleGateLang() {\n    gateLang = gateLang === 'en' ? 'ar' : 'en';\n    document.querySelectorAll('#gate .t[data-en]').forEach(el => {\n      el.textContent = el.dataset[gateLang === 'ar' ? 'ar' : 'en'];\n    });\n    const inp = document.getElementById('gate-input');\n    inp.placeholder = gateLang === 'ar' ? inp.dataset.arPh : inp.dataset.enPh;\n    document.getElementById('gate-lang-btn').textContent = gateLang === 'en' ? 'عربي' : 'English';\n    document.documentElement.lang = gateLang;\n    document.documentElement.dir = gateLang === 'ar' ? 'rtl' : 'ltr';\n  }\n\n  function checkHomePass() {\n    const input = document.getElementById('gate-input');\n    const field = document.getElementById('gate-field');\n    const err = document.getElementById('gate-error');\n    if (input.value.trim().toLowerCase() === HOME_PASS) {\n      sessionStorage.setItem(HOME_AUTH_KEY, 'yes');\n      const l = document.getElementById('loader');\n      if (l) {\n        l.dataset.hiding = '1';\n        l.style.display = 'none';\n      }\n      document.documentElement.classList.remove('gate-locked');\n      homeGate.classList.add('hide');\n      homeGate.style.display = 'none';\n      document.documentElement.lang = 'en';\n      document.documentElement.dir = 'ltr';\n      return;\n    }\n    input.value = '';\n    err.textContent = gateLang === 'ar' ? 'ليس هذا السرّ ♡' : 'That\\'s not the secret ♡';\n    field.classList.remove('shake');\n    void field.offsetWidth;\n    field.classList.add('shake');\n    setTimeout(() => { err.textContent = ''; }, 2400);\n  }\n\n  document.getElementById('gate-input').addEventListener('keydown', e => {\n    if (e.key === 'Enter') checkHomePass();\n  });\n  if (sessionStorage.getItem(HOME_AUTH_KEY) === 'yes') {\n    document.documentElement.classList.remove('gate-locked');\n    homeGate.classList.add('hide');\n  }\n\n  // ══ LOADER ══\n  function hideLoader() {\n    const l = document.getElementById('loader');\n    if (!l || l.dataset.hiding) return;\n    l.dataset.hiding = '1';\n    if (document.hidden) {\n      l.style.display = 'none';\n    } else {\n      l.style.transition = 'opacity .7s ease';\n      l.style.opacity = '0';\n      setTimeout(() => { l.style.display = 'none'; }, 750);\n    }\n  }\n  // Hard backup — always hides after 2.5s even if load event never fires\n  setTimeout(hideLoader, 2500);\n  // Normal path — hide 1.4s after all resources finish\n  window.addEventListener('load', () => setTimeout(hideLoader, 1400));\n  // If timeout fired while tab was hidden, hide the moment tab gets focus\n  document.addEventListener('visibilitychange', () => {\n    if (!document.hidden) hideLoader();\n  });\n\n  // ══ CUSTOM CURSOR ══\n  const dot  = document.getElementById('cur-dot');\n  const ring = document.getElementById('cur-ring');\n  let mx = -100, my = -100, rx = -100, ry = -100;\n\n  document.addEventListener('mousemove', e => {\n    mx = e.clientX; my = e.clientY;\n    dot.style.left = mx + 'px';\n    dot.style.top  = my + 'px';\n    maybeSparkle(mx, my);\n  });\n\n  (function ringLoop() {\n    rx += (mx - rx) * .14;\n    ry += (my - ry) * .14;\n    ring.style.left = rx + 'px';\n    ring.style.top  = ry + 'px';\n    requestAnimationFrame(ringLoop);\n  })();\n\n  document.querySelectorAll('a,button,input,textarea').forEach(el => {\n    el.addEventListener('mouseenter', () => {\n      ring.style.width  = '44px';\n      ring.style.height = '44px';\n    });\n    el.addEventListener('mouseleave', () => {\n      ring.style.width  = '28px';\n      ring.style.height = '28px';\n    });\n  });\n\n  // ══ SPARKLES ══\n  const CHARS = ['✦','✧','✺','·','⋆'];\n  let lastSparkle = 0;\n\n  function maybeSparkle(x, y) {\n    const now = Date.now();\n    if (now - lastSparkle < 55) return;\n    lastSparkle = now;\n    for (let i = 0; i < 2; i++) {\n      const el = document.createElement('div');\n      el.className = 'sparkle';\n      el.textContent = CHARS[Math.floor(Math.random() * CHARS.length)];\n      el.style.left     = (x + (Math.random() - .5) * 18) + 'px';\n      el.style.top      = (y + (Math.random() - .5) * 18) + 'px';\n      el.style.fontSize = (8 + Math.random() * 9) + 'px';\n      el.style.setProperty('--dx', (Math.random() - .5) * 32 + 'px');\n      el.style.animationDuration = (.6 + Math.random() * .4) + 's';\n      document.body.appendChild(el);\n      setTimeout(() => el.remove(), 1000);\n    }\n  }\n\n  // ══ LANG TOGGLE ══\n  let currentLang = 'en';\n\n  const HEADINGS = {\n    en: {\n      story:   'Our <em>Story</em>',\n      streak:  'Your <em>Journey</em>',\n      reasons: 'Reasons I <em>Love You</em>',\n      letter:  'A <em>Letter</em> to You',\n    },\n    ar: {\n      story:   'قصّتنا <em>المشتركة</em>',\n      streak:  'رحلتكم <em>إلى اليوم</em>',\n      reasons: 'أسباب <em>حبي لكِ</em>',\n      letter:  'رسالة <em>إليكِ</em>',\n    }\n  };\n\n  function toggleLang() {\n    currentLang = currentLang === 'en' ? 'ar' : 'en';\n    const html = document.documentElement;\n    html.lang = currentLang;\n    html.dir  = currentLang === 'ar' ? 'rtl' : 'ltr';\n\n    // Update all simple text nodes\n    document.querySelectorAll('.t[data-en]').forEach(el => {\n      el.textContent = el.dataset[currentLang];\n    });\n\n    // Update complex headings with em tags\n    document.getElementById('heading-story').innerHTML   = HEADINGS[currentLang].story;\n    document.getElementById('heading-streak').innerHTML  = HEADINGS[currentLang].streak;\n    document.getElementById('heading-reasons').innerHTML = HEADINGS[currentLang].reasons;\n    document.getElementById('heading-letter').innerHTML  = HEADINGS[currentLang].letter;\n\n    // Update placeholders\n    document.querySelectorAll('[data-en-ph]').forEach(el => {\n      el.placeholder = currentLang === 'ar' ? el.dataset.arPh : el.dataset.enPh;\n    });\n\n    // Update monogram and hero names\n    const isAr = currentLang === 'ar';\n    document.getElementById('monogram-circle').textContent = isAr ? 'أ & أ' : 'A & A';\n    document.getElementById('hero-names').innerHTML = isAr\n      ? 'أحمد <em>&amp;</em> ألاء'\n      : 'Ahmed <em>&amp;</em> Alaa';\n\n    // Toggle button label\n    document.getElementById('lang-btn').textContent = isAr ? 'English' : 'عربي';\n  }\n\n  // ══ HOME DETAILS (letter date, daily thought, tab title) ══\n  (function initHomeDetailsForAlaa() {\n    const TZ = 'Africa/Cairo';\n\n    function cairoTodayKey() {\n      const parts = new Intl.DateTimeFormat('en-CA', {\n        timeZone: TZ,\n        year: 'numeric',\n        month: '2-digit',\n        day: '2-digit'\n      }).formatToParts(new Date());\n      const o = {};\n      for (const p of parts) o[p.type] = p.value;\n      return `${o.year}-${o.month}-${o.day}`;\n    }\n\n    function cairoLongDates() {\n      const en = new Intl.DateTimeFormat('en-GB', {\n        timeZone: TZ,\n        weekday: 'long',\n        day: 'numeric',\n        month: 'long',\n        year: 'numeric'\n      }).format(new Date());\n      const ar = new Intl.DateTimeFormat('ar-EG', {\n        timeZone: TZ,\n        weekday: 'long',\n        day: 'numeric',\n        month: 'long',\n        year: 'numeric'\n      }).format(new Date());\n      return { en, ar };\n    }\n\n    const ps = document.getElementById('letter-ps-line');\n    if (ps) {\n      const { en, ar } = cairoLongDates();\n      ps.dataset.en = \"P.S. — I'm thinking of you today · \" + en;\n      ps.dataset.ar = 'ملاحظة — أفكر فيكِ اليوم · ' + ar;\n      ps.textContent = document.documentElement.lang === 'ar' ? ps.dataset.ar : ps.dataset.en;\n    }\n\n    const whisper = document.getElementById('daily-whisper-line');\n    const titles = document.querySelectorAll('#reasons-grid .reason-title.t[data-en]');\n    if (whisper && titles.length) {\n      const day = cairoTodayKey();\n      let idx = NaN;\n      try {\n        if (sessionStorage.getItem('home_whisper_day') === day) {\n          idx = parseInt(sessionStorage.getItem('home_whisper_idx'), 10);\n        }\n      } catch {}\n      if (isNaN(idx) || idx < 0 || idx >= titles.length) {\n        idx = Math.floor(Math.random() * titles.length);\n        try {\n          sessionStorage.setItem('home_whisper_day', day);\n          sessionStorage.setItem('home_whisper_idx', String(idx));\n        } catch {}\n      }\n      const pick = titles[idx];\n      whisper.dataset.en = pick.dataset.en || '';\n      whisper.dataset.ar = pick.dataset.ar || '';\n      whisper.textContent = document.documentElement.lang === 'ar' ? whisper.dataset.ar : whisper.dataset.en;\n    }\n\n    const baseTitle = document.title;\n    setTimeout(() => {\n      if (document.title === baseTitle) document.title = baseTitle + ' · for Alaa';\n    }, 10000);\n  })();\n\n  // ══ COUNTDOWN ══\n  const TARGET = new Date('2026-08-25T00:00:00');\n  const MILESTONES = new Set([300, 200, 100, 50, 30, 14, 7, 1]);\n  const shownMilestones = new Set();\n\n  function pad(n) { return String(n).padStart(2, '0'); }\n\n  function tick() {\n    const now  = new Date();\n    const diff = TARGET - now;\n\n    // Wedding day!\n    if (diff <= 0) {\n      showWeddingDay();\n      return;\n    }\n\n    const days  = Math.floor(diff / 86400000);\n    const hours = Math.floor((diff % 86400000) / 3600000);\n    const mins  = Math.floor((diff % 3600000)  / 60000);\n    const secs  = Math.floor((diff % 60000)    / 1000);\n\n    document.getElementById('cd-days').textContent  = pad(days);\n    document.getElementById('cd-hours').textContent = pad(hours);\n    document.getElementById('cd-mins').textContent  = pad(mins);\n    document.getElementById('cd-secs').textContent  = pad(secs);\n\n    document.getElementById('stat-sleeps').textContent   = days;\n    document.getElementById('stat-weekends').textContent = Math.floor(days / 7) + Math.ceil((days % 7) / 5);\n    document.getElementById('stat-fridays').textContent  = Math.ceil(days / 7);\n\n    // Milestone check (once per session per milestone)\n    if (MILESTONES.has(days) && !shownMilestones.has(days)) {\n      shownMilestones.add(days);\n      showMilestone(days);\n    }\n  }\n\n  tick();\n  setInterval(tick, 1000);\n\n  // ══ MILESTONE BANNER ══\n  let milestoneTimer;\n\n  const MILESTONE_EN = d => `✦ Only ${d} day${d === 1 ? '' : 's'} to go — the countdown is almost over!`;\n  const MILESTONE_AR = d => `✦ ${d} يوم${d === 1 ? '' : ''} فقط — العد التنازلي يقترب من نهايته!`;\n\n  function showMilestone(days) {\n    clearTimeout(milestoneTimer);\n    const banner = document.getElementById('milestone-banner');\n    document.getElementById('milestone-text').textContent =\n      currentLang === 'ar' ? MILESTONE_AR(days) : MILESTONE_EN(days);\n    banner.classList.add('show');\n    milestoneTimer = setTimeout(closeMilestone, 7000);\n  }\n\n  function closeMilestone() {\n    document.getElementById('milestone-banner').classList.remove('show');\n  }\n\n  // Demo milestone: press Shift+M\n  document.addEventListener('keydown', e => {\n    if (e.shiftKey && e.key === 'M') showMilestone(100);\n    if (e.shiftKey && e.key === 'C') startConfetti();\n    if (e.shiftKey && e.key === 'W') showWeddingDay();\n  });\n\n  // ══ WEDDING DAY ══\n  function showWeddingDay() {\n    const section = document.getElementById('countdown-section');\n    section.innerHTML = `\n      <div class=\"celebration-wrap\">\n        <div class=\"celebration-title\">Today Is <em>The Day!</em></div>\n        <div class=\"celebration-sub\">Ahmed &amp; Alaa — August 25, 2026</div>\n      </div>`;\n    startConfetti();\n  }\n\n  // ══ CONFETTI ══\n  const confettiCanvas = document.getElementById('confetti-canvas');\n  const cctx = confettiCanvas.getContext('2d');\n  let cW, cH, cPieces = [], cActive = false;\n\n  function resizeConfetti() {\n    cW = confettiCanvas.width  = innerWidth;\n    cH = confettiCanvas.height = innerHeight;\n  }\n  resizeConfetti();\n  window.addEventListener('resize', resizeConfetti);\n\n  const CONF_COLORS = ['#B8975A','#D4B483','#EEE0C4','#FFFFFF','#F7F4EF','#C9A96E','#fff8ee'];\n\n  function ConfettiPiece() {\n    this.x    = Math.random() * cW;\n    this.y    = Math.random() * -cH;\n    this.w    = 5 + Math.random() * 9;\n    this.h    = 3 + Math.random() * 5;\n    this.col  = CONF_COLORS[Math.floor(Math.random() * CONF_COLORS.length)];\n    this.rot  = Math.random() * Math.PI * 2;\n    this.drot = (Math.random() - .5) * .14;\n    this.vy   = 2 + Math.random() * 3;\n    this.vx   = (Math.random() - .5) * 1.5;\n    this.update = function() {\n      this.x += this.vx; this.y += this.vy; this.rot += this.drot;\n      if (this.y > cH + 20) { this.y = -20; this.x = Math.random() * cW; }\n    };\n    this.draw = function() {\n      cctx.save();\n      cctx.translate(this.x, this.y);\n      cctx.rotate(this.rot);\n      cctx.fillStyle = this.col;\n      cctx.fillRect(-this.w / 2, -this.h / 2, this.w, this.h);\n      cctx.restore();\n    };\n  }\n\n  function startConfetti() {\n    confettiCanvas.style.display = 'block';\n    cActive = true;\n    cPieces = [];\n    for (let i = 0; i < 160; i++) cPieces.push(new ConfettiPiece());\n    confettiLoop();\n    setTimeout(stopConfetti, 9000);\n  }\n\n  function stopConfetti() {\n    cActive = false;\n    cPieces = [];\n    confettiCanvas.style.display = 'none';\n  }\n\n  function confettiLoop() {\n    if (!cActive) return;\n    cctx.clearRect(0, 0, cW, cH);\n    cPieces.forEach(p => { p.update(); p.draw(); });\n    requestAnimationFrame(confettiLoop);\n  }\n\n  // ══ PARALLAX + SCROLL PROGRESS + HERO FADE ══\n  const heroBg = document.getElementById('hero-bg');\n  const heroContent = document.querySelector('.hero-content');\n  const scrollCue = document.querySelector('.scroll-cue');\n  const heroSection = document.querySelector('.hero');\n  const progressBar = document.getElementById('scroll-progress');\n  const mobileMQ = window.matchMedia('(max-width: 600px)');\n  const reduceMotion = window.matchMedia('(prefers-reduced-motion: reduce)');\n\n  let scrollTicking = false;\n  function onScroll() {\n    const sy = window.scrollY;\n    const isMobile = mobileMQ.matches;\n\n    // hero photo parallax (desktop only — see mobile face-crop fix above)\n    if (heroBg) {\n      if (isMobile) heroBg.style.transform = '';\n      else heroBg.style.transform = `scale(1.08) translateY(${sy * 0.3}px)`;\n    }\n\n    // hero content gentle fade-out + slight rise as you scroll past the hero\n    if (heroContent && heroSection && !reduceMotion.matches) {\n      const heroH = heroSection.offsetHeight || window.innerHeight;\n      const p = Math.min(1, Math.max(0, sy / (heroH * 0.85)));\n      heroContent.style.opacity = String(1 - p);\n      heroContent.style.transform = `translateY(${-p * 24}px)`;\n      if (scrollCue) scrollCue.style.opacity = String(1 - Math.min(1, sy / 220));\n    }\n\n    // gold scroll-progress bar at top of viewport\n    if (progressBar) {\n      const docH = document.documentElement.scrollHeight - window.innerHeight;\n      const pct = docH > 0 ? Math.min(100, Math.max(0, (sy / docH) * 100)) : 0;\n      progressBar.style.width = pct + '%';\n      progressBar.classList.toggle('active', sy > 40);\n    }\n\n    scrollTicking = false;\n  }\n  window.addEventListener('scroll', () => {\n    if (scrollTicking) return;\n    scrollTicking = true;\n    requestAnimationFrame(onScroll);\n  }, { passive: true });\n  mobileMQ.addEventListener('change', e => { if (e.matches && heroBg) heroBg.style.transform = ''; });\n  // run once so progress bar reflects current position on load\n  onScroll();\n\n  // ══ SCROLL REVEAL ══\n  const revealObs = new IntersectionObserver(entries => {\n    entries.forEach(e => { if (e.isIntersecting) { e.target.classList.add('visible'); revealObs.unobserve(e.target); } });\n  }, { threshold: 0.12 });\n  document.querySelectorAll('.reveal').forEach(el => revealObs.observe(el));\n\n  // ══ REASONS STAGGER REVEAL ══\n  const reasonObs = new IntersectionObserver(entries => {\n    entries.forEach(entry => {\n      if (entry.isIntersecting) {\n        const items = entry.target.querySelectorAll('.reason-item');\n        items.forEach((item, i) => {\n          setTimeout(() => item.classList.add('revealed'), i * 160);\n        });\n        reasonObs.unobserve(entry.target);\n      }\n    });\n  }, { threshold: 0.08 });\n  const reasonsGrid = document.getElementById('reasons-grid');\n  if (reasonsGrid) reasonObs.observe(reasonsGrid);\n\n  // ══ STREAK WIDGET ══\n  (function() {\n    const JB_KEY      = '$2a$10$KbNF/mCJtyHlUvHzwELTKOdQ0Sybn.GKVOW/1S8XJqeZQrYSwULwq';\n    const JB_VISITS   = '69efe653856a6821897d39fd';\n    const WEDDING  = new Date('2026-08-25T00:00:00');\n    const PROPOSAL = new Date('2025-12-09T00:00:00');\n    const TODAY    = new Date(); TODAY.setHours(0,0,0,0);\n\n    function ds(d) { return d.toISOString().slice(0,10); }\n    function addD(d,n) { const r=new Date(d); r.setDate(r.getDate()+n); return r; }\n\n    /** Virtual “visited” from engagement day through today (display only; not written to JSONBin as backfill). */\n    function baselineVisitedSet() {\n      const s = new Set();\n      let d = new Date(PROPOSAL);\n      while (d <= TODAY) { s.add(ds(d)); d = addD(d, 1); }\n      return s;\n    }\n\n    function mergeDisplayVisited(storageSet) {\n      return new Set([...storageSet, ...baselineVisitedSet()]);\n    }\n\n    let storageSet;\n    try {\n      storageSet = new Set(JSON.parse(localStorage.getItem('streak_visits') || '[]'));\n    } catch { storageSet = new Set(); }\n\n    const todayStr = ds(TODAY);\n    const isNewVisit = !storageSet.has(todayStr);\n    storageSet.add(todayStr);\n    const arrSorted = [...storageSet].sort();\n    localStorage.setItem('streak_visits', JSON.stringify(arrSorted));\n\n    if (isNewVisit) {\n      fetch('/api/jsonbin', { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ binId: JB_VISITS, payload: { dates: arrSorted } }) }).catch(() => {});\n    }\n\n    let visited = mergeDisplayVisited(storageSet);\n\n    function currentStreak() {\n      let n = 0, d = new Date(TODAY);\n      while (visited.has(ds(d))) { n++; d = addD(d, -1); }\n      return n;\n    }\n    function bestStreak() {\n      const sorted = [...visited].sort();\n      let best = 0, cur = 0, prev = null;\n      for (const s of sorted) {\n        const d = new Date(s + 'T12:00:00');\n        cur = prev && (d - prev) === 86400000 ? cur + 1 : 1;\n        best = Math.max(best, cur); prev = d;\n      }\n      return best;\n    }\n\n    // Grid bounds: align to Monday\n    const gridStart = new Date(PROPOSAL);\n    const dow = gridStart.getDay();\n    gridStart.setDate(gridStart.getDate() - (dow === 0 ? 6 : dow - 1));\n\n    const gridEnd = new Date(WEDDING);\n    const dowE = gridEnd.getDay();\n    if (dowE !== 0) gridEnd.setDate(gridEnd.getDate() + (7 - dowE));\n\n    const cols = [], months = [];\n    let cur = new Date(gridStart), colIdx = 0, todayCol = -1;\n    while (cur <= gridEnd) {\n      const week = [];\n      for (let i = 0; i < 7; i++) {\n        week.push(new Date(cur));\n        cur = addD(cur, 1);\n      }\n      const mLabel = week[0].getMonth() !== months[months.length - 1]?.month\n        ? { col: colIdx, month: week[0].getMonth(), label: week[0].toLocaleString('en', { month: 'short' }) }\n        : null;\n      if (mLabel) months.push(mLabel);\n      if (week.some(d => ds(d) === todayStr)) todayCol = colIdx;\n      cols.push(week);\n      colIdx++;\n    }\n\n    const weddingStr = ds(WEDDING);\n    const TILE = 15;\n\n    const monthRow = document.getElementById('streak-month-row');\n    for (let c = 0; c < cols.length; c++) {\n      const cell = document.createElement('div');\n      cell.className = 'streak-month-cell';\n      cell.style.width = '12px';\n      const m = months.find(x => x.col === c);\n      cell.textContent = m ? m.label : '';\n      monthRow.appendChild(cell);\n      if (c < cols.length - 1) {\n        const g = document.createElement('div');\n        g.style.width = '3px';\n        g.style.flexShrink = '0';\n        monthRow.appendChild(g);\n      }\n    }\n\n    const dayLabels = document.getElementById('streak-day-labels');\n    ['M', '', 'W', '', 'F', '', 'S'].forEach(l => {\n      const el = document.createElement('div');\n      el.className = 'streak-day-label';\n      el.textContent = l;\n      dayLabels.appendChild(el);\n    });\n\n    const grid = document.getElementById('streak-grid');\n\n    function paintTiles() {\n      grid.innerHTML = '';\n      cols.forEach(week => {\n        const col = document.createElement('div');\n        col.className = 'streak-col';\n        week.forEach(day => {\n          const tile = document.createElement('div');\n          tile.className = 'streak-tile';\n          const d = ds(day);\n          const inRange = day >= PROPOSAL && day <= WEDDING;\n          if (!inRange) tile.classList.add('s-out');\n          else if (d === weddingStr) { tile.classList.add('s-wedding'); tile.textContent = '♡'; }\n          else if (d === todayStr) tile.classList.add('s-today');\n          else if (day > TODAY) tile.classList.add('s-future');\n          else if (visited.has(d)) tile.classList.add('s-visited');\n          else tile.classList.add('s-missed');\n          tile.setAttribute('title', day.toLocaleDateString('en', { month: 'short', day: 'numeric', year: 'numeric' }));\n          col.appendChild(tile);\n        });\n        grid.appendChild(col);\n      });\n    }\n\n    function updateStats() {\n      document.getElementById('streak-current').textContent = currentStreak();\n      document.getElementById('streak-longest').textContent = bestStreak();\n      document.getElementById('streak-total').textContent = visited.size;\n      const totalDays = Math.round((WEDDING - PROPOSAL) / 86400000);\n      const elapsed = Math.max(0, Math.min(totalDays, Math.round((TODAY - PROPOSAL) / 86400000)));\n      const pct = Math.round(elapsed / totalDays * 100);\n      document.getElementById('streak-pct').textContent = pct + '%';\n      setTimeout(() => { document.getElementById('streak-fill').style.width = pct + '%'; }, 400);\n    }\n\n    paintTiles();\n    updateStats();\n\n    if (todayCol > 0) {\n      const scroll = document.getElementById('streak-scroll');\n      setTimeout(() => { scroll.scrollLeft = Math.max(0, (todayCol - 8) * TILE); }, 300);\n    }\n\n    fetch(`/api/jsonbin?binId=${JB_VISITS}`)\n      .then(r => (r.ok ? r.json() : Promise.reject()))\n      .then(j => {\n        const remote = j.record?.dates || [];\n        const merged = new Set([...remote, ...JSON.parse(localStorage.getItem('streak_visits') || '[]')]);\n        merged.add(todayStr);\n        localStorage.setItem('streak_visits', JSON.stringify([...merged].sort()));\n        visited = mergeDisplayVisited(merged);\n        paintTiles();\n        updateStats();\n      })\n      .catch(() => {});\n  })();\n\n  // ══ PETALS ══\n  const canvas = document.getElementById('petals-canvas');\n  const ctx    = canvas.getContext('2d');\n  let W, H;\n  function resize() { W = canvas.width = innerWidth; H = canvas.height = innerHeight; }\n  resize();\n  window.addEventListener('resize', resize);\n  const PCOLS = ['rgba(184,151,90,.18)','rgba(212,180,131,.22)','rgba(238,224,196,.28)','rgba(255,255,255,.12)'];\n  function Petal() {\n    this.reset = function(init) {\n      this.x = Math.random() * W; this.y = init ? Math.random() * H : -16;\n      this.r = 3 + Math.random() * 6; this.rot = Math.random() * Math.PI * 2;\n      this.drot = (Math.random() - .5) * .03; this.vy = .5 + Math.random() * 1;\n      this.vx = (Math.random() - .5) * .6; this.col = PCOLS[Math.floor(Math.random() * PCOLS.length)];\n      this.wave = Math.random() * Math.PI * 2;\n    };\n    this.reset(true);\n    this.update = function() {\n      this.wave += .02; this.x += this.vx + Math.sin(this.wave) * .4;\n      this.y += this.vy; this.rot += this.drot;\n      if (this.y > H + 20) this.reset(false);\n    };\n    this.draw = function() {\n      ctx.save(); ctx.translate(this.x, this.y); ctx.rotate(this.rot);\n      ctx.beginPath(); ctx.ellipse(0, 0, this.r, this.r * .5, 0, 0, Math.PI * 2);\n      ctx.fillStyle = this.col; ctx.fill(); ctx.restore();\n    };\n  }\n  const petals = Array.from({length: 30}, () => new Petal());\n  (function loop() {\n    ctx.clearRect(0, 0, W, H);\n    petals.forEach(p => { p.update(); p.draw(); });\n    requestAnimationFrame(loop);\n  })();"
];

export function runIndexScripts(root: HTMLElement) {
  for (const code of indexScripts) {
    const el = document.createElement("script");
    el.text = code;
    root.appendChild(el);
  }
}
